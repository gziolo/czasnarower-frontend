define([ 'js/mixins/with_data_grid', 'js/mixins/with_storage', 'js/mixins/with_template' ], function(WithDataGrid, WithStorage, WithTemplate) {
  'use strict';

  return {
    WithDataGrid : WithDataGrid,
    WithStorage : WithStorage,
    WithTemplate : WithTemplate
  };
});
